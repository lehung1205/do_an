@model int
@if (Model > 0)
{
    <span class="badge bg-danger rounded-pill ms-1 nav-chat-unread" id="navChatUnreadBadge" title="Tin nhắn chưa đọc">@Model</span>
}
else
{
    <span class="d-none nav-chat-unread" id="navChatUnreadBadge"></span>
}
