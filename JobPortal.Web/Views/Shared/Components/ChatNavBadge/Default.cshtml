@model int
@if (Model > 0)
{
    <span class="site-nav-icon-badge nav-chat-unread" id="navChatUnreadBadge" title="Tin nhắn chưa đọc">@Model</span>
}
else
{
    <span class="d-none nav-chat-unread site-nav-icon-badge" id="navChatUnreadBadge"></span>
}
